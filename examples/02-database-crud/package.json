{
  "name": "task-manager-api-02-database",
  "version": "1.0.0",
  "private": true,
  "description": "Task Manager API — snapshot at the end of Step 8 (CRUD backed by PostgreSQL via TypeORM)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0",
    "pg": "^8.12.0",
    "reflect-metadata": "^0.2.2",
    "typeorm": "^0.3.20"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
